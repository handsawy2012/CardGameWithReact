function player(){
    
    return {
            name: "PLAYER" + (i+1),
            points: 0, 
            cardsOnHand : null,
            tempPoints : 0,
            pass : false
        };

    this.players;
}

export default players;